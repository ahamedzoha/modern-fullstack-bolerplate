{
  "Servers": {
    "1": {
      "Name": "Docker Postgres",
      "Group": "Servers",
      "Host": "postgres_db",
      "Port": 5432,
      "MaintenanceDB": "postgres",
      "Username": "${POSTGRES_USER}",
      "Password": "${POSTGRES_PASSWORD}",
      "SSLMode": "prefer"
    }
  }
}
